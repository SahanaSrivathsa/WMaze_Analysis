Execute these commands in this directory to run the analysis for Session Data:

chmod u+x analyzeData.sh
export PATH=$PATH:/path/to/script
analyzeData.sh -{analysis type}


[Analysis types: ‘overall’, ‘inbound’, and ‘outbound’]
